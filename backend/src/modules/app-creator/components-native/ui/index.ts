/**
 * React Native UI Component Generators
 *
 * Exports all UI component generators for React Native.
 * These generators create React Native components using:
 * - View, Text, TouchableOpacity, etc.
 * - @expo/vector-icons (Ionicons)
 * - react-native-safe-area-context
 */

export {
  generateButton,
  generateCard,
  generateInput,
  generateAvatar,
  generateEmptyState,
  generateLoadingSpinner,
  generateModal,
  generateToast,
  generateConfirmDialog,
  generateActionSheet,
  generateProgressBar,
} from './ui.generator';
